name: update-hosts

on: 
  schedule:
    - cron: "10 1/3 * * *"
  workflow_dispatch:
    # 定义手动触发时的输入参数
    inputs:
      doh_provider:
        description: '选择 DNS-over-HTTPS 服务商'
        required: true
        default: 'cloudflare'
        type: choice
        options:
          - cloudflare
          - google
          - aliyun

jobs:
  hosts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python 3
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Init Env
        run : |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          sudo timedatectl set-timezone "Asia/Shanghai"
          python -m pip install --upgrade pip
          python -m pip install requests retry
          
      - name: Get Github Hosts
        shell: python
        # 将输入参数转为环境变量供 Python 读取
        env:
          CHOSEN_PROVIDER: ${{ github.event.inputs.doh_provider || 'cloudflare' }}
        run: |
          import os
          import time
          import requests
          from retry import retry
          from typing import Any, Optional

          @retry(tries=3, delay=2)
          def get_ip(session: Any, github_url: str, provider: str) -> Optional[str]:
              providers = {
                  'cloudflare': {
                      'url': 'https://cloudflare-dns.com/query',
                      'headers': {'accept': 'application/dns-json'}
                  },
                  'google': {
                      'url': 'https://dns.google/resolve',
                      'headers': {}
                  },
                  'aliyun': {
                      'url': 'https://dns.alicdn.com/resolve',
                      'headers': {}
                  }
              }

              conf = providers.get(provider, providers['cloudflare'])
              params = {'name': github_url, 'type': 'A'}
              
              try:
                  rs = session.get(conf['url'], params=params, headers=conf['headers'], timeout=10)
                  data = rs.json()
                  if 'Answer' in data:
                      for answer in data['Answer']:
                          if answer['type'] == 1:
                              return answer['data']
                  raise Exception(f"No A record found via {provider}")
              except Exception as ex:
                  print(f"[{provider}] Error resolving {github_url}: {ex}")
                  raise ex

          if __name__ == '__main__':
              # 获取选择的服务商，如果自动执行则默认为 cloudflare
              current_provider = os.getenv('CHOSEN_PROVIDER', 'cloudflare')
              print(f"Using DoH Provider: {current_provider}")

              session = requests.Session()
              
              gitHubDoMains = [
                  "alive.github.com", "api.github.com", "api.individual.githubcopilot.com",
                  "avatars.githubusercontent.com", "avatars0.githubusercontent.com",
                  "avatars1.githubusercontent.com", "avatars2.githubusercontent.com",
                  "avatars3.githubusercontent.com", "avatars4.githubusercontent.com",
                  "avatars5.githubusercontent.com", "camo.githubusercontent.com",
                  "central.github.com", "cloud.githubusercontent.com", "codeload.github.com",
                  "collector.github.com", "desktop.githubusercontent.com",
                  "favicons.githubusercontent.com", "gist.github.com",
                  "github-cloud.s3.amazonaws.com", "github-com.s3.amazonaws.com",
                  "github-production-release-asset-2e65be.s3.amazonaws.com",
                  "github-production-repository-file-5c1aeb.s3.amazonaws.com",
                  "github-production-user-asset-6210df.s3.amazonaws.com", "github.blog",
                  "github.com", "github.community", "github.githubassets.com",
                  "github.global.ssl.fastly.net", "github.io", "github.map.fastly.net",
                  "githubstatus.com", "live.github.com", "media.githubusercontent.com",
                  "objects.githubusercontent.com", "pipelines.actions.githubusercontent.com",
                  "raw.githubusercontent.com", "user-images.githubusercontent.com",
                  "vscode.dev", "education.github.com", "private-user-images.githubusercontent.com"
              ]
              
              dockerDoMains = ["hub.docker.com", "ghcr.io", "gcr.io", "k8s.gcr.io", "quay.io"]

              dnsList = []
              dnsList.append('# Varcli Hosts Start')
              dnsList.append(f'# Update at: {time.strftime("%Y-%m-%d %H:%M:%S")}')
              dnsList.append(f'# Source: DoH ({current_provider})')

              for label, domains in [("GitHub", gitHubDoMains), ("Docker", dockerDoMains)]:
                  dnsList.append(f'\n# {label} Hosts')
                  for domain in domains:
                      try:
                          ip = get_ip(session, domain, current_provider)
                          if ip:
                              print(f"Success: {domain} -> {ip}")
                              dnsList.append(f"{ip.ljust(20)} {domain}")
                      except:
                          print(f"Final Fail: {domain}")

              dnsList.append('\n# Varcli Hosts End')
              
              with open('hosts', 'w', encoding="utf-8") as f:
                  f.write('\n'.join(dnsList))

      - name: Check and Push
        run: |
          if [ -n "$(git status -s | grep hosts)" ]; then
            git add hosts
            git commit -m "update via ${{ github.event.inputs.doh_provider || 'auto-scheduler' }} $(date +%Y-%m-%d" "%H:%M:%S)"
            git push -f
          fi
